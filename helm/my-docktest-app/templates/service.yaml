apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-docktest-app.fullname" . }}
  labels:
    {{- include "my-docktest-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-docktest-app.selectorLabels" . | nindent 4 }}
